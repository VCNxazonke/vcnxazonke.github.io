/**
 * index.js — placeholder (Firebase integration reserved for future use)
 * Firebase SDK initialisation has been moved to a future module-based setup.
 * The ES-module import statements that were here caused a SyntaxError when
 * loaded as a plain <script> tag, so this file is intentionally kept clean.
 */